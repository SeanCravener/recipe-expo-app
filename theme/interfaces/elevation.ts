import { ViewStyle } from "react-native";

export interface Elevation {
  level0: ViewStyle;
  level1: ViewStyle;
  level2: ViewStyle;
  level3: ViewStyle;
  level4: ViewStyle;
  level5: ViewStyle;
}
